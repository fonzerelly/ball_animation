{
  "name": "ball_animation",
  "version": "1.0.0",
  "description": "Simple example application implemented in ng1 and ng2",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "npm --prefix ./ng1_version install ./ng1_version && npm --prefix ./ng2_version install ./ng2_version",
    "start": "node node_modules/http-server/bin/http-server"
  },
  "author": "Christian Hörauf",
  "license": "MIT",
  "dependencies": {
    "http-server": "^0.9.0"
  }
}
